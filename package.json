{
  "name": "bot-mudae-likes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "./node_modules/.bin/prisma generate && (tsc || exit 1) && node ./dist/server.js",
    "dev": "nodemon -e ts  --exec \"npm run compile\" "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^2.26.0",
    "discord.js": "^12.5.3",
    "dotenv": "^10.0.0",
    "moment": "^2.29.1"
  },
  "devDependencies": {
    "@types/node": "^16.0.0",
    "nodemon": "^2.0.7",
    "prisma": "^2.26.0",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.5"
  }
}
